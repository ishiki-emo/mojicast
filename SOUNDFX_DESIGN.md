# 音イベント演出 設計メモ（ROADMAP #7）

笑い声・拍手・歓声などを検出して、画面の好きな場所に画像やエフェクトを出す。

**方針: ワード演出とは独立させる。** 供給源（何が起きたか）も出力先（何を出すか）も
別系統にし、片方を消してももう片方が動く状態を保つ。将来まとめたくなったら
寄せられるが、最初から共通化すると「単語が絡む不具合」と「音が絡む不具合」が
同じコードで混ざる。

---

## 1. なぜ感情ではなく音イベントなのか（決定済み・2026-08-17）

SenseVoice の感情推定は3条件すべてで実用に届かず不採用。代わりに
sherpa-onnx 同梱の AudioTagging（AudioSet 527クラス）を使う。詳細は ROADMAP #7。

| | 感情（SenseVoice SER） | 音イベント（AudioTagging） |
|---|---|---|
| 実配信での検出 | 14%・大半が EMO_UNKNOWN | **笑い5/5・誤検出0** |
| 出力 | ラベル1個 | 確率つき527クラス |
| 安定性 | 切り出し境界±100msで31%変わる | ノイズ床ほぼ0（中央値0.00/95%点0.14） |
| モデル | ASRに内蔵＝**多言語モデル限定** | **独立**。k2-ja でも同じに動く |
| 追加コスト | — | 27MB / RSS +115MB / CPU 4.8% |

**モデル非依存なのが効いている。** 日本語特化（k2-ja）の利用者にもそのまま届く。

---

## 2. 全体像

```
音声ブロック ─┬─→ VAD → ASR → 字幕（既存。ワード演出はこの中）
              │
              └─→ soundfx.SoundFxDetector → on_sound_event(group, score, speaker)
                     ↑ VADを通さない。笑い声は「発話」と見なされず切り出されないため
                                            │
                                            ▼
                              app_server: broadcast({"type":"sound", ...})
                                            │ SSE
                                            ▼
                              overlay.html: 設定に従って画像/エフェクトを出す
```

既存の `on_final` / `on_partial` とは**別のコールバック・別のSSEイベント種別**にする。
字幕テキストを経由しないので、ワード演出の実装（`FX.buildTable` /
`FX.renderWords` / `data-particle`）には一切触らない。

---

## 3. モジュール構成

### 3.1 `soundfx.py`（新規）

`punct.py` / `translate.py` / `vrcchat.py` と同じ「engine から呼ばれる独立部品」の位置づけ。
**engine 以外に依存しない**（wordstore / voicecmd / punct を import しない）。

```python
GROUPS = {                     # AudioSetクラス → 演出グループ
    "笑い": {"Laughter", "Giggle", "Snicker", "Belly laugh",
             "Chuckle, chortle", "Baby laughter"},
    "拍手・歓声": {"Applause", "Clapping", "Cheering"},
    "歌・口笛": {"Singing", "Male singing", "Female singing",
                 "Child singing", "Humming", "Whistling"},
    "泣き": {"Crying, sobbing", "Whimper", "Sniff"},
    "驚き・叫び": {"Screaming", "Shout", "Yell", "Squeal", "Gasp"},
    "咳・くしゃみ": {"Cough", "Sneeze", "Throat clearing"},
}

class SoundFxDetector:
    """音声ブロックを受け取り、グループが立ったらコールバックする。
    話者ごとに1インスタンス（バッファとクールダウンが独立するため）。
    分類器（Recognizer相当）は重いので全話者で共有する。"""
    def __init__(self, tagger, on_event, speaker="", cfg=None): ...
    def feed(self, block: np.ndarray) -> None: ...
```

判定パラメータは実測で確定済み（`bench/probe_emotion_live.py` と同値。
根拠はコミット 5337e78）。**この数字は勘で動かさない。**

| 項目 | 値 | 根拠 |
|---|---|---|
| 窓 | 1.5秒 | 3秒だとピーク半減 |
| 刻み | 0.25秒 | 0.5秒だと山を踏み外し 1.14→0.65 |
| 入口しきい値 ENTER | 0.3 | 0.15まで下げると非笑いを拾う |
| 発火しきい値 FIRE（笑い） | 0.5 | 単一しきい値だと窓の位相ずれで際どい非笑いが出入りする（位相次第で 0.28→0.46 まで動いた）。本物の笑いのピークは 0.56〜1.49 なので 0.5 が分離線。ENTER で追跡を始めピークが FIRE に達した時点で発火（強い笑いは遅延なし・弱い立ち上がりは0.25〜0.5秒遅れ） |
| 咳・くしゃみ | 0.5 | 咳と笑いは互いのスコアを立て合う |
| 驚き・叫び | 0.25 | 表現により弱く出る |
| 同時発火 | 最上位グループのみ | 取り違え時も正解側のスコアが常に高い |
| クールダウン | 1.0秒 | 2.0秒だと別々の笑いが1回に統合される |

### 3.2 `engine.py` への差し込み

`_stream_loop` の中、**VADへ渡しているのと同じブロック**を分岐させるだけ。

```python
# 既存
while len(buffer) >= WINDOW_SIZE:
    chunk = buffer[:WINDOW_SIZE]
    vad.accept_waveform(chunk)
    if self._soundfx is not None:      # ← 追加はこの2行
        self._soundfx[speaker].feed(chunk)
    buffer = buffer[WINDOW_SIZE:]
```

- ASRモデルの種類を見ない（`caps` 分岐は不要）
- 無効時は `self._soundfx is None` で完全にゼロコスト。**モデルもロードしない**
- コールバック `on_sound_event(group, score, speaker)` を1本追加

### 3.3 `app_server.py`

```python
on_sound_event=lambda g, s, spk="": broadcast(
    {"type": "sound", "group": g, "score": round(s, 2), "speaker": spk}),
```

`_engine_on_final` のようなテキスト加工は挟まない。**ボイスコマンド判定も通さない**
（音イベントは言葉ではないので、コマンド誤発動の経路に載せない）。

### 3.4 `overlay.html`

`handle()` に `case "sound":` を1つ足す。既存の `final` / `partial` の描画とは
別レイヤー（`position:fixed` の独立コンテナ）に出すので、字幕行のDOMに干渉しない。

```javascript
case "sound": SFX.play(msg.group, msg.speaker); break;
```

### 3.5 `ui/soundfx.js`（新規）

`fx.js` とは別ファイルにする。`fx.js` は「字幕テキストの装飾」が責務で、
こちらは「画面の任意座標に単発の演出を出す」で、責務が違う。

fx.js から借りるのは次の2つ**だけ**（逆依存は作らない）:

1. **パーティクル発射**（公開APIの `FX.spawnParticles(el, type, color)`）—
   DOM要素基準だが、演出用に置いた画像要素をそのまま target に渡せば済む
   （粒サイズは要素の fontSize で決まるので、それも画像サイズから設定する）
2. **キーフレームCSS** — WORD_ANIMS のアニメは大半が transform/filter 系なので
   `<img>` にもそのまま効く。クラスを付けるだけで「単語エフェクトと同じ動き」になる

| WORD_ANIMS | 画像に流用 |
|---|---|
| pop / bounce / shake / spin / flash / glowpulse / heartbeat / float / glitch / neon | **○ そのまま使える**（transform / filter / opacity 系） |
| rainbow / shine / wave | ✕ テキスト専用（グラデ文字・文字分解が前提）。UIの選択肢から外す |

つまり「点滅・光る・大きさが変わる系」の要望は、**新規実装ではなくクラス流用**で
満たせる。soundfx.js が新たに持つのは「画像を指定座標に出して消す」導線と、
登場/退場アニメ（fade / pop-in / drop 等。LINE_ANIMS の語彙を踏襲）だけ。

---

## 4. 設定

`data/config.json` に独立したキーを足す。`word_fx`（既存のワード演出フラグ）とは別。

```jsonc
"sound_fx": false,              // 既定OFF。ONにして初めて27MBをDLしロードする
"sound_fx_rules": {
  "笑い": {
    "on": true,
    // --- 出すもの（併用可。排他にしない） ---
    "images": ["laugh1.png", "laugh2.png"],  // data/soundfx/ 内。複数ならランダム
    "particle": "confetti",     // fx.js の PARTICLES と同じ語彙。"none" 可
    // --- 置き方 ---
    "pos": {"x": 0.8, "y": 0.2},// 画面比率（解像度非依存）。OBSの配置と独立
    "size": 0.25,               // 画面幅に対する比率（アスペクト比は維持）
    "jitter": 0.05,             // 表示のたび位置をこの範囲で散らす（0=固定）
    // --- 動き（単語エフェクトと同じ語彙） ---
    "enter": "pop",             // 登場: pop | fade | drop | slide | none
    "anim": "heartbeat",        // 表示中: WORD_ANIMS の流用可能セット + none
    "duration": 1500,           // 表示時間ms
    // --- 発火制御 ---
    "min_score": 0.0,           // 検出しきい値への上乗せ（0=既定のまま）
    "scale_by_score": true      // スコアで強弱（下記）
  },
  "拍手・歓声": { "on": false }
}
```

- **既定OFF**。低スペック機（TECH_GUIDE で非推奨とした N100 等）に黙って
  CPU 4.8% を足さない
- **画像とパーティクルは併用可**にする（排他の action 方式はやめた）。
  「画像が跳ねながら紙吹雪も出る」が1ルールで書ける
- 話者別（自分/ゲスト）の出し分けは**初版では持たない**。ルールは全話者共通にし、
  イベントに speaker が乗っているので後から足せる形にしておく

### 4.1 ユーザー画像の置き場と配信

現状の app_server は `/ui/` と個別パスしか配っておらず、**ユーザー画像を
overlay へ届ける経路が存在しない**。次を追加する:

- 置き場: `DATA_BASE/soundfx/`（プロファイルではなくアプリ共通。画像は使い回すため）
- 配信: `GET /soundfx/<name>`。**basename のみ許可**（パストラバーサル対策。
  `test_app_server_security.py` に検査を足す）
- 登録: 設定UIから「ファイルを選ぶ」→ サーバが `data/soundfx/` へコピー
  （`POST /api/soundfx/image`）。GIF/APNG はそのまま動く（`<img>` なので）
- 形式は png / jpg / gif / webp。上限は 5MB 程度（overlay は配信PCローカルなので
  帯域は問題にならないが、無制限にはしない）

### 4.2 スコア連動の強弱（確率が取れることを活かす）

感情ラベルと違い**スコアが連続値で取れる**のがこの方式の強み。
`scale_by_score: true` のとき:

- パーティクル数: スコア 0.3→標準 / 1.0以上→2倍
- 画像サイズ: `size` を基準に 1.0〜1.3倍
- 実測の分布（笑い 0.31〜2.18）に合わせ、0.3〜1.5 を 0〜1 に正規化して使う

「小さく笑うと小さく、爆笑すると盛大に」が設定なしで付いてくる。

### 4.3 発火の多重制御（detector 側のクールダウンとは別の層）

- **表示中に同グループが再発火**: 表示を延長（duration リセット）。
  リスタートで点滅させない・無視して空白も作らない、の中間
- **グループ間の同時発火**: そのまま重ねる（exclusive 判定で1窓1グループに
  絞られているので、実際に重なるのは時間差のケースのみ）
- **レート上限**: 同グループ 10秒に3回まで。連続爆笑で画面が画像で埋まる事故を防ぐ。
  超過分は演出だけ抑制し、イベント自体は流す（overlay 側の判断で抑制）

---

## 5. 分離を保つための約束

1. `soundfx.py` は wordstore / voicecmd / punct を import しない
2. 音イベントは `on_final` を経由しない（＝字幕テキストにならない・ログにも残らない）
3. SSE のイベント種別を分ける（`"sound"` は `"final"` と混ぜない）
4. `ui/soundfx.js` は `fx.js` から `PARTICLE_DEFS` だけ借りる。逆依存は作らない
5. `sound_fx: false` のとき、モデルのDL・ロード・推論のすべてが起きない

この5つを守れば、音イベント演出をまるごと削除しても既存機能は無傷で残る。

---

## 5.5 設定UI（studio 新ピラー「サウンド演出」）

ワード演出（words.html の fx モード）と**同じ見た目の文法**で作る。使い方を
覚え直させない。ただしコードは別ペイン（iframe）で、共有はCSSの見た目だけ。

```
┌ サウンド演出 ────────────────────────────────┐
│ [✔] 😄 笑い       画像:[laugh.png ▾][+]  紙吹雪▾  動き: ドキドキ▾  [テスト] │
│ [ ] 👏 拍手・歓声  画像:[なし      ▾][+]  キラキラ▾ 動き: ポップ▾   [テスト] │
│ [ ] 🎵 歌・口笛    …                                                   │
│   └ 選択中: 位置 x[━━●━] y[●━━━]  大きさ[━●━━]  表示時間[1.5s]        │
│      ┌────────プレビュー枠（16:9）────────┐                            │
│      │        （クリックで位置を直接指定）  │                            │
│      └──────────────────────────────┘                            │
└──────────────────────────────────────────┘
```

- **［テスト］ボタンが作り込みの本丸**。`POST /api/soundfx/test {"group": "笑い"}`
  → 通常と同じ SSE `"sound"` を流す。**実際に笑わなくても** OBS に取り込んだ
  本物の overlay で調整できる（エンジン停止中でも動く）
- 位置指定は 16:9 のプレビュー枠へのクリックで直接置ける＋スライダー微調整
- プレビュー枠には現在の字幕ボックス位置も薄く描く（字幕と被る配置に気づける）

## 5.6 その他の作り込み案（初版に入れる/入れないの仕分け）

| 案 | 初版 | メモ |
|---|---|---|
| 複数画像ランダム | **入れる** | `images` 配列にするだけ。飽き防止に効く |
| GIF/APNG アニメ画像 | **入れる** | `<img>` なので実装コストゼロ。動く画像はここで賄える |
| スコア連動強弱（4.2） | **入れる** | この方式にしか出せない味 |
| テスト発火（5.5） | **入れる** | これが無いと調整のたびに笑うことになる |
| 位置ジッター | **入れる** | 1行で済み、固定位置の作業感が消える |
| 効果音を鳴らす | 見送り | overlay は OBS 内で音が出せるが、配信音声設計に踏み込むので保留 |
| 話者別ルール | 見送り | イベントに speaker は乗せておく（§4） |
| ゲーム効果音トリガー（Gunshot 等） | 見送り | グループ定義の追加だけで済む形にはしておく |
| mojipack での演出セット共有 | 見送り | 画像同梱の権利まわりを #1 と合わせて設計してから |

---

## 6. 実装順序

| # | 内容 | 確認方法 |
|---|---|---|
| 1 | `soundfx.py`（検出ロジック＋テスト） | 録音済みwavで笑い5/5を再現 |
| 2 | engine 差し込み＋コールバック | 実マイクでコンソールにイベントが出る |
| 3 | SSE ブロードキャスト＋**テスト発火API** | `POST /api/soundfx/test` で `type:"sound"` が届く |
| 4 | `ui/soundfx.js`＋overlay 描画（パーティクルのみ） | テスト発火で紙吹雪が出る |
| 5 | 画像配信経路（4.1）＋画像・位置・サイズ・アニメ描画 | 自作画像が指定座標に指定の動きで出る |
| 6 | 設定UI（studio 新ピラー・5.5） | GUIから登録し［テスト］で確認できる |

テスト発火APIを3に前倒ししてある。**4以降の描画の作り込みが全部
「笑わずに」回せる**ようになり、開発も後のユーザー調整も同じ道具で済む。
1〜4 で「笑うと画面に何か出る」が成立する。5〜6 が当初構想の
「オリジナル画像を任意の場所に出す」の本体。

---

## 7. 未確定・要検討

- **音源の切り分け**: 分類器は「誰の音か」を区別しない。BGMのボーカルを
  `歌・口笛` として拾う（実測済み）。マイク入力側なら混入しないので、
  ソースごとに有効グループを変えられると良い（例: ゲスト側は歌を無効）。
  初版は「ソロ配信のマイク入力」を主対象とし、この問題を設計でなく
  運用（歌グループをOFFにする）で逃がす
- **低スペック機の逃げ道**: 刻みを0.5秒にすればCPUは半分。検出精度と引き換え
- **サンプル数**: 笑いの正解照合は実配信1本・5サンプル。実装後、実配信で
  回しながら追加検証したい（テスト発火とは別に、実検出のログを残す口を
  detector に付けておくと照合が楽）
