# Mojicast 依存パッケージ（Python 3.11 で検証）
#
#   pip install -r requirements.txt
#
# ※ ReazonSpeech k2-asr パッケージは不要になりました（v0.4系:
#   推論ラッパーを engine.py へインライン化。モデル本体はHFから自動DL）。
# ※ torch / transformers も不要（v0.3系: 句読点=ONNX Runtime、
#   翻訳=CTranslate2 へ移行済み）。モデルを変換し直す開発作業のときだけ
#   tools/convert_models.py のコメントに従って別途インストールしてください。

huggingface_hub==1.22.0
sherpa-onnx==1.13.4
onnxruntime==1.27.0
ctranslate2==4.8.1
sentencepiece==0.2.1
opencc==1.4.1
numpy==2.4.6
pywebview==6.2.1
pythonnet==3.1.0
sounddevice==0.5.5

# パッケージング（配布用exeを作る場合のみ）
# pyinstaller==6.21.0
