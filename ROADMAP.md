# Mojicast ロードマップ

将来の機能・改善の構想メモ。優先度や内容はテスターのフィードバックで見直す。
（最終更新: 2026-08-24 / 現行リリース: v0.9.6）

**v0.9.6（翻訳の忠実性＋表示の不具合）:** **言っていないことを字幕に出さない**方向の修正。
訳せなかった行の残骸（実配信1600行で韓国語10.1%・中国語2.2%）と、原文に無い罵倒語が
湧いた英訳（同4637行で0.19%）は訳ごと捨てて原文（日本語）へ倒す。**英訳辞書の固有名詞を
中国語・韓国語の訳でも保護**（英訳が大文字始まりの語が対象。おるか→Oruka→오루카と音写
されて残る）。**字幕が無言で消える経路を塞ぐ**（訳を出せなかった行は原文へ倒し、英訳の
連続失敗と音声の途絶を警告バーに出す。音声が無反応になったら自動で開き直す。下の
「不具合: 稼働中に字幕が無言で全消失する」）。タイプライター表示で英訳の単語がくっつく
不具合（スマートグラス風・配布された EvenG2 が該当）と、古いCPUで全文字に「。」が付く
不具合（int8が壊れる環境は自動で fp32 へ切替）も修正。早口・辞書登録・翻訳の限界について
画面の案内も足した。**モデルの差し替えは無し＝既存利用者の再DLは不要**。

**v0.9.5（パフォーマンス）:** 句読点BERTと英訳モデルの int8 化で**常駐メモリを大幅削減**
（標準構成 741→**507MB** / 英訳ON 1167→**748MB** / 全部盛り 1801→**1379MB**）。
処理も句読点2倍速・英訳3倍速。英訳の品質修正（反復暴走の抑止・文頭が小文字になる問題・
配信スラング9語の用語辞書）、**確定した字幕だけ表示する設定**（テスター要望・途中経過の
デコードごと省くのでCPUも軽い）、配布物の不要ファイル除去（Zip 87→**82MB**）。

**v0.9.0（リアクション演出＋窓サイズ）:** リアクション演出（笑い声・拍手・歌などの
音イベント検出→画像/パーティクル表示。実験的機能・既定OFF・27MB追加DL・
画像ごとの位置/大きさ/回転・テスト発火・#7調査の副産物）、GUI窓の大きさ選択と
窓の位置・サイズ記憶、Discordコミュニティ導線。修正: リリック字幕の翻訳併記(#5)・
表示切替で前の字幕が残る(#6)・中国語訳の反復暴走(#7)・多言語モードの数字空白・
GUI縮小時のフッター浮き

**v0.8.0（翻訳のみ字幕＋VRChat連携）:** 字幕の表示モード「翻訳のみ」（デザイン単位・
訳文を本文サイズ・フル装飾で表示・翻訳OFF時は併記へ自動フォールバック・#2）、
VRChatチャットボックスへの字幕送信（OSC/UDP自前実装・文字起こし/訳文選択・
1.6秒まとめ送り・タイピング表示・コックピットにトグル）、エフェクト単語の
ASCII単語境界ガード（英文の語中誤爆防止）、ライセンス総点検対応（CREDITS全面整備・
LICENSE同梱・cuDNN/PIL除去・アプリ内クレジット導線）、コックピット右カラムの
スタジオ入口をsticky化＋起動時の窓高拡大、マニュアル・ガイドの追随更新。

**v0.7.0（ボイスコマンド＋シーン）:** ボイスコマンド（ユーザー定義ウェイクワード・
レイアウト/デザイン/シーン/翻訳切替・おまかせ・カスタムコマンド作成UI＝言い回し→8動作）、
シーン（デザイン＋レイアウトのセット切替・調整中→上書きUX・既定3シーン）、
字幕の自動消去オプション（テスター要望・既定OFF）。コックピットの呼称整理
（字幕の見た目／デザイン／レイアウト／単語セット）。コラボ中のコマンドは自分の声のみ。
macOS対応の土台も同梱: OS依存を platform_compat へ集約（フォント/CPU/言語/UIスケール/
ダイアログ）・コラボはWin限定化（macはUIグレーアウト＋サーバ側ガード）・
requirements の環境マーカー化・GitHub Actions で Win/mac 両CIを開始（#10参照）。

**v0.6.2（セキュリティ・安定性パッチ）:** ローカルAPIへの外部Originアクセスと
DNSリバインディングを遮断。認識エンジンの多重起動を状態管理で防止し、音声・翻訳・
SSEの待ち行列とHTTP本文へ安全な上限を追加。音声は約30秒の余裕を持って保持し、
通常配信の体験を変えずに異常時のメモリ増加を抑える。ワード演出ON/OFFの即時反映表示も修正。

**v0.6.1（パッチ）:** UI刷新で混入した回帰を修正 — 初回モデルDL中にコックピットの状態表示が
「ロード中...」のままで進捗（○○/約△△MB）が出なかった問題（`setState` が loading/error 時に
`detail` を捨てていた）。あわせてDL失敗メッセージの表示も復活。

**v0.6.0 で追加（v0.5.0 からの主な変更）:** VTuber向けにUIを全面再設計（コックピット／スタジオ／
アプリ設定の3画面構成・「今日の字幕」・配信プレビュー⇄字幕ログ切替・GUI既定をライトへ）、
GUI表示言語切替（日本語/中文/English・i18n）、リリックビデオ風字幕（lyric_lab）、
中国語地域別（簡体/台湾/香港）・インドネシア語翻訳、更新のお知らせ機能（バナー＋
アプリ設定「情報・連絡先」の更新を確認・`latest.json` 比較・`open_external`）、
情報・連絡先画面（バージョン表示・GitHub / X への導線）、免責・注意事項の明記、
アプリアイコン埋め込み、切断系トレースバックの静音化。次は Booth 出品（下記#2 と並行）。

## 正式版（v1.0）の判断基準

ベータ運用（v0.4系〜）で以下を満たしたら正式版として公開する:

1. **実戦フィードバックの反映** — テスターと自分の常用配信で出た改善点を消化しきる
2. **UI/UXのさらなる最適化** — 迷いなく使える導線（初見のオンボーディング・設定の
   分かりやすさ・エラー時の案内）を突き詰める
3. **CPU差の吸収** — /api/perf の実測収集 → スレッド自動チューニング・
   自動パフォーマンスモード等で「適性◎〜△のCPUなら設定いじらずそれなりに動く」状態に
   （候補メニュー: ORT更新 / コラボ時ゲスト薄文字オフ / SenseVoiceハイブリッド。
   Intel/AMD別ビルドは最終手段）
4. **配布形態の完成** — Inno Setupインストーラ化（下記#2）・Booth出品（無料＋応援版、
   応援版特典は限定mojipack案）

---

## 1. mojipack: 単語系の共有対応（コミュニティ化の入口）
**規模: 中 / スタイル分は v0.3.0 で実装済み → 残りは単語系**

- フェーズ1の残り: 単語系（hotwords / effects / banned / glossary / **プロファイル丸ごと**）の
  エクスポート/インポート
  - プロファイルがそのまま共有単位（v0.3.0のプロファイル構造を利用）
  - 仕組みは実装済みのスタイル版を踏襲: `.mojipack`（実体はJSON）・マージ方式
    （上書きしない・ID再生成・名前衝突は「〜 (imported)」）・値の無害化とエスケープ
  - フォントはライセンス上同梱不可（フォント名参照＋フォールバックの現挙動を維持）
- フェーズ2: リポジトリに `community-packs/` + GitHub Discussions で募集
- フェーズ3: アプリ内ギャラリー（GitHub上のインデックス取得・急がない）

## 2. Inno Setup インストーラ化
**規模: 小〜中（≈1日）**

- 配布物が setup.exe 1個になり、展開の遅さ・MOTW・フォルダの威圧感が構造的に解消
- v0.3.0のONNX/CT2移行と、その後の不要ファイル除去（2026-08-21）で
  **731ファイル/0.20GB・Zip 82MB** まで軽くなったため緊急度は下がったが、
  テスターの導入体験としては依然価値あり
- onefile化は非推奨（この規模は起動毎のtemp展開が遅すぎる）

## 3. 話題レーダー: 配信の流れをキーワードで見せる
**規模: 中 / v1.0 の目玉候補（2026-08-21 方針転換: 別アプリ+LLM → 本体+埋め込み /
2026-08-22 オフライン PoC 完了）**

確定字幕の蓄積から「いま何について話しているか」「ここまでどんな流れだったか」を
**キーワードで**表示する。文章の要約はしない（＝LLM不要）。

**体験イメージ（2026-08-21）**: 配信を続けるうちにログが溜まり、その時点での
「いまの話題」がキーワードで出る。話題が切り替わったら新しいキーワードを追加していき、
**累積3〜5個**を表示する。狙いは**視聴者向け**で、途中から見に来た人が「いま何の話か」
「ここまで何を話していたか」を一目で掴めて**置いてけぼりにならない**こと。
画面配置は検討中だが、いくつかプリセットを用意する方向。

- **手法は埋め込みベース**: 発話を小型の埋め込みモデルでベクトル化し、隣接区間の
  類似度が落ちた点を話題の切れ目として検出 → 区間ごとの代表キーワードを出す。
  「①雑談 → ②ゲームの話 → ③告知」のような章立てになる
- キーワードは TF-IDF 等で「その区間で特徴的な語」を選ぶ。**過去ログ（logs/）を
  背景コーパスにできる**のが強み（配信者ごとの口癖が自然に除外される）。
  **実測で裏付けが取れた（2026-08-22）**: 過去ログ1329発話を分母に入れると、
  「ウィンドウズ」「マック」のような常用語が下がり、その回だけの固有名詞
  （番組名など）が上がる。式そのものより背景コーパスの有無のほうが効いた
- **語の切り出しをどうするかは未決**。いまの PoC は fugashi + unidic-lite を
  使っているが、**辞書がディスク248MB**あり、配布Zip 82MB に対して重すぎる
  （RSS は +36MB で問題ない）。候補は (a) 小さい辞書（ipadic ≒50MB / sudachi small）、
  (b) 辞書を任意DLの追加機能扱いにする（モデル非同梱DL方式と同じ流儀）。
  埋め込みのトークナイザ流用は品詞が取れないので、句にする機能を捨てることになる
- **メモリ（2026-08-22 実測ずみ）**: 日本語特化は
  [cl-nagoya/ruri-v3-30m](https://huggingface.co/cl-nagoya/ruri-v3-30m)、
  多言語は `intfloat/multilingual-e5-small`。両方 ONNX 化して測った
  （手順・数字の詳細は `bench/topic_radar/README.md`）:

  | | ファイル計 | RSS | 速度 |
  | --- | --- | --- | --- |
  | Ruri v3 30m（日本語特化） | **40MB** | **+126MB** | 0.65ms/発話 |
  | e5-small（多言語） | **123MB** | **+219MB** | 1.29ms/発話 |

  90分の配信ぶん（1405発話）を1〜2秒で処理できるので、リアルタイム性は問題ない。
  変換で効いたのは2点: **量子化は Gather も対象にする**（埋め込み行列が重みの
  大半なので、MatMul だけだと Ruri 148→116MB にしか減らない。Gather 込みで 38MB）、
  **tokenizer は tokenizer.json ではなく SentencePiece を直に読む**（同じ ID が
  出るのに RSS が 1/6。e5 で +250MB → +42MB）。sentencepiece は翻訳で既に
  積んでいるので追加依存なし
- **多言語ユーザーには Ruri では足りない**（日本語特化のため）。中国語圏の要望が
  多言語対応（#6）の発端だった経緯からも、海外配信者では機能しない状態は避けたい。
  ただし **e5 は日本語でも Ruri の9割弱の質**を出す（実配信4本・343分での実測）ので、
  多言語モードで日本語配信をしても大きくは劣化しない。
  gte-multilingual-base は153MB、bge-m3 は568MBで重い
- **使い分けは既存パターンに乗せる**: 認識（k2-ja ⇄ SenseVoice）・翻訳（FuguMT ⇄ M2M）と
  同じく「日本語特化 ⇄ 多言語」の2択にし、使う方だけDL・常駐させる（不要モデルの
  自動解放も既存）。
  **訂正（2026-08-22）**: 以前ここに「多言語モードは句読点BERTを積まない（383MB）ので
  e5 が乗っても標準構成より軽いまま」と書いていたが、これはファイルサイズの話。
  RSS では **e5 +219MB > 句読点BERT +154MB** なので、多言語モードのほうが重くなる
- **ベクトルDBは不要**（Faiss等のミドルウェアは要らないという意味で、埋め込みモデル
  自体は要る）。配信1回ぶん（数千発話×数百次元）は数MBの行列なので numpy の行列積で
  検索が一瞬で終わる
- 表示先: **OBSオーバーレイが主**（視聴者に見せるのが目的）。字幕のデザイン/レイアウト
  機構（preset/box）をそのまま流用できるはずで、シーン（#8）に組み込めば「歌枠では
  出さない・雑談では出す」も自然にできる。コックピット（配信者が流れを把握）と
  アーカイブの章分けは副次的な使い道
- **チューニングは設定で開ける**（既定値は持つ）。話題がころころ変わる人と、1つの話を
  じっくりする人で最適値が違うため、**話題の切り替わりやすさ（類似度のしきい値）**と
  **表示個数**を可変にする。「話し終わりの区切り」「一区切りの最長」と同じスライダー
  方式に揃えれば学習コストが低い。切り替わりが頻繁すぎて読めなくなるのを防ぐ
  **最短保持時間**のガードも要りそう
- **既定値は実測で決めた（2026-08-22・PoC完了）**。実配信4本（343分・4839発話）で
  オフライン検証した。PoC 一式は `bench/topic_radar/`（ログ本体は個人の配信内容なので
  Git 管理外）。分かったことのうち、実装時に効くもの:

  - **窓は発話数ではなく時間で持つ**。実配信は 15.6発話/分、合成ログは 5.7発話/分と
    3倍近く違い、同じ「30発話」が別物になる。既定は**直近8分**
  - **切れ目のしきい値は固定値にしない**。埋め込みの類似度は配信ごとにも配信中にも
    分布が動く（90分で +0.27 → -0.03）。**直近400回の下位5%**を採るようにすると
    感度が揃い、更新の3分の2が「話が変わったから」になった（固定値では1%）
  - **類似度はそのまま使えない。中心化が要る**。話し言葉は共通成分が強く、生のまま
    だと何を比べても 0.96〜0.98 に張り付く。その時点までの平均ベクトルを引くと
    切れ目 0.01 / それ以外 0.17 まで開く（語彙の重なりだけでは 0/7 しか拾えない）
  - **埋め込みはキーワードの重み付けにも効く**。「その語が出てくる発話が区間の
    セントロイドにどれくらい近いか」で重みを付けると、脇道の語が落ちて、話題語だけで
    終わらず述語まで付く率が 27% → 57% に上がる
  - 既定値: 窓8分 / 30秒ごとに評価 / 最短保持3分 / 切れ目=下位5% → **4〜5分に1回**の更新
- **句にするのは日本語モードだけ**。「ラーメン」ではなく「ラーメン食べに行こう」と
  出すために、話題語＋それにかかる述語を元の発話から切り出している（要約を生成
  しないので、本人が言っていないことは出ない）。ただしこれは日本語の語順（述語が
  後ろ）と unidic の品詞・書字形基本形に完全に依存するので、多言語モードは
  キーワードのみになる。日本語でも句になるのは57%なので、体験が半分になるわけではない
- 発展: 話題語を `/api/effects` に流して字幕内を自動ハイライト（API既存）
- **旧構想（不採用・2026-07-12〜2026-08-21）**: 別アプリ＋小型ローカルLLM
  （Qwen3-1.7B・llama.cpp/GGUF・SSE購読で本体改修ゼロ）で話題語を生成する案だった。
  CPU実試験で実用性は確認済みだったが、**LLMは +1GB級でメモリ削減（#12）と逆行する**
  ため見送り。将来「文章で要約」が欲しくなったら再検討の余地はある

## 4. コラボ字幕・案B（各自ローカル＋リレー共有・多人数対応）
**規模: 中〜大 / 詳細設計は [COLLAB_DESIGN.md](COLLAB_DESIGN.md) / 案A改（1対1）は v0.4.0 で実装済み**

各自がMojicastを起動し、**確定字幕テキストだけ**をリレー共有する多人数対応の本命構成。

- 各自のクリーンなマイク音声でローカルASR → 精度最高・話者帰属完璧・負荷分散。
  共有するのはテキストのみで帯域ほぼゼロ・多人数可（v0.4.0の1対1制限を超える）
- リレー: Cloudflare Workers + Durable Objects の小さなWebSocketリレー
  （部屋コード方式・無料枠で十分。数十行規模）
- 素朴な代替: Cloudflare Tunnel / Tailscale でホストのAPIへゲストが直接POST
  （サーバー運用ゼロ・ホストのOBSに全員分表示）
- 受信側の改修は小さい: 話者付き描画基盤（話者→スタイル/ボックス割当・overlay複数ペイン）は
  **v0.4.0 で実装済み**なので、リモート字幕を既存パイプラインへ流すだけ
- スタイル共有（相手の見た目=本人のプリセット）は COLLAB_DESIGN.md の #4b 参照

## 6. 多言語対応（モデル切替の「受け皿」）
**規模: 中 / 主要部は実装済み（2026-07-22・feat/asr-multilingual→v0.5.0候補）**

Twitterで中国語圏からの要望（2026-07-21）を起点に、ASR・翻訳をモデル選択式にした。

- **実装済み**: モデルレジストリ受け皿（k2/SenseVoice＋能力表→engineが後処理を自動切替）・
  SenseVoice多言語認識（中英日韓粤・軽量モード兼用: 13700HXで翻訳込み13〜20%、
  N100で条件付き動作）・翻訳4方向（FuguMT＋M2M-100、認識言語×翻訳先で経路自動選択・
  広東語はzh扱い）・読み欄「／」複数形の置換強化・AIモデル設定窓・
  初回おすすめ設定（OS言語/CPU検出・ユーザー了解制）・不要モデルの自動解放
- **残り**: 韓国語訳の品質改善（現状「試験的」表記）・多言語運用の実戦フィードバック消化
- 将来オプション: 翻訳品質の上位枠に MADLAD-400 3B（Apache-2.0・メモリ3GB級・
  16GB機向け）。NLLBは CC-BY-NC のため不採用
- **言語別ユーザー辞書（組み込み配信用語のユーザー拡充）**: 現状「英訳を固定する単語」＝
  ja→英のユーザー辞書は、組み込み `_STREAM_TERMS`（translate.py）と同じ「翻訳前の
  source側置換」機構で動く。これを中国語・インドネシア語など**M2M方向にも開放**する案
  （言語別 glossary を translate_m2m の事前置換へ）。
  - 壁: ja→英はラテン文字passthroughで堅牢だが、M2Mは注入した訳語を再翻訳して壊す語がある
    （実測: 歌回→歌曲回归・点赞→赞赞）。ユーザー任意登録には**登録時プレビュー/壊れ検証**が要る
  - 保存はプロファイル単位にし、#1 の mojipack単語系共有に載せれば「配信用語パック」を配れる
- 感情・音声イベントの活用は #7 参照

## 7. 音声由来のインタラクション（構想メモ・寝かせ枠 2026-07-22）
**規模: 感情エフェクト=小〜中 / ボイスコマンド=中（誤発動設計が本体）**

- **感情・音声イベント→エフェクト**: SenseVoiceは認識と同時に感情（喜怒哀楽）と
  音声イベント（笑い・拍手・咳等）のトークンを出せる（sherpa-onnxの結果に露出済み）。
  「笑い→(笑)＋パーティクル」「HAPPY→字幕がポップに」等のエモーショナル字幕。
  単語エフェクトの既存基盤（spawnParticles / effects API）にそのまま乗る。
  - **2026-08-16/17 実測 → 感情トリガーは見送り**。調査ツール
    `bench/probe_emotion_live.py`（マイク/Chromeループバック/コラボ2系統・録音付き）。
    取れるラベルは HAPPY/SAD/ANGRY/NEUTRAL/FEARFUL/DISGUSTED/SURPRISED/OTHER/
    EMO_UNKNOWN の9種、音声イベントは Speech/BGM/Laughter/Applause/Cry/Sneeze/
    Breath/Cough/Sing 等。`stream.result` の `.emotion` / `.event` / `.lang` に露出。
    - 3条件とも大半が EMO_UNKNOWN: ナレーション16発話=25% / 大げさな演技37発話=24% /
      **感情豊かな実配信59発話=14%**（HAPPY3・DISGUSTED1・NEUTRAL4、BGMが20件混入）。
      感情が最も乗っている実配信で打率が最低。ANGRYはどの条件でも0件
    - **同じ録音のまま切り出し境界を±100msずらすと5/16(31%)でラベルが変わる**。
      演技のブレではなくモデル側の不安定さ。トリガーには使えない
    - 「出ない」より「**出たり出なかったりする**」のが演出として致命的。
      画像・エフェクトを出す用途は**テキスト(単語)トリガー**で作るのが正解
      （感情が取れなかった発話でも「すごい」「ダメだ」等は文字では全部取れている）
    - 使うとしても、単語トリガーで発火させた上に乗せる**修飾子**（出たら派手になる）
      までが限度。その場合は境界を数パターン変えた多数決で揺れを減らせる
  - **落とし穴**: `engine._recognize` の0.9秒無音パディング（k2の精度用）を通すと
    感情ラベルが**100% EMO_UNKNOWN に潰れる**。2026-08-17に caps["pad"] で
    モデル別に切替（k2=あり/SenseVoice=なし）。副次的に数字まわりの余分な空白も解消
    （`2026 年 7 月4 日`→`2026年7月4日`）。回帰テストは `RecognizePaddingTests`
  - 未調査: 日本語以外（中国語等）でのSER精度・CPUで動く代替SERモデル
  - 制約: 多言語モデル選択時のみ
- **ボイスコマンド**: 確定字幕のテキストマッチで「フレーズ→アクション」
  （クリア・翻訳オン・エフェクト発射・プロファイル切替等）。実装自体は小さいが、
  設計の本体は誤発動対策（呼びかけ語プレフィックス必須・コマンド文は字幕に出さない・
  確定ベースなので数秒の遅延がある前提の用途選び）
  - **2026-07-24 実装開始（feat/voice-command）**: ウェイクワード方式（ユーザー定義・
    ゆれ表記対応・認識誘導へ自動登録）で、レイアウト切替（名前/番号/おまかせ）・
    字幕デザイン切替（同）・翻訳切替（オン/オフ・翻訳先＋動作中は自動再起動）・
    シーン切替・**カスタムコマンド作成UI**（言い回し→動作の自由登録。シーン/デザイン/
    レイアウト/おまかせ/翻訳/字幕クリアの8動作・組み込みより優先）まで実装。
    残り候補: コマンドセットのmojipack共有
- どちらも #9 カスタムエフェクトと組み合わせると化ける（例: 笑い検出で自作エフェクト発動）

## 8. シーン: 字幕デザイン＋レイアウトのセット切替 → **v0.7.0 で実装済み**
**規模: 小〜中 / 本体は実装済み（2026-07-24 実機フィードバック → v0.7.0）**

- **実装済み**: 「歌枠」「雑談」「ゲーム」のような名前で **字幕デザイン(preset)＋
  レイアウト(box) の組**を保存し、ワンタップ／ボイスコマンド（#7）で一括切替。
  既定3シーン・調整中→上書きUX・データは `scenes.json`
- 呼称: 単語プロファイルは「単語セット」に改称済み（2026-07-24・旧「配信セット」）。
  本機能は「シーン」（OBSのシーン概念とも直感が揃う）。空いた「配信セット」の名は
  将来の「シーン＋単語＋翻訳のまるごとセット」用に温存
- **残り（発展形）**: 単語セット・翻訳設定・ワード演出ON/OFFまで含めた
  「まるごと配信モード」。mojipack共有（#1）とも好相性（見た目一式を配れる）

## 9. カスタムパーティクル / カスタムエフェクト
**規模: パーティクル=小 / エフェクト段階1=中 / 遠い将来枠**

ユーザーが「飛ばすもの」と「エフェクト」を自作できるようにする。
実装順は **パーティクル → エフェクト段階1 → 段階2**（小さい成功体験から積む）。

- **カスタムパーティクル（小）**: 実体は fx.js の `PARTICLE_DEFS`（絵文字・個数・
  飛び方・回転のJSON）なので、単語スタジオに4項目の小フォームを足すだけ。
  絵文字ならフォント同梱問題もなし
- **エフェクト段階1: レシピ型（中）**: 既存プリミティブのパラメータ開放
  （アニメ周期・グラデ色・グロー幅など）＋名前を付けて保存。
  `--fx-grad` CSS変数化済みでグラデ色の下地はあり。実体がJSONなので mojipack と好相性
- **エフェクト段階2: キーフレーム開放**: transform/opacity/filter の時系列JSONから
  `@keyframes` を生成（構造化データからCSSを組み立てる＝注入リスクを段階1と同等に維持）
- **生CSS/JSプラグインは開けない**（mojipack共有時のセキュリティホールになるため）

## 10. Mac版（Apple Silicon）→ **v0.9.0 で対応済み**
**規模: ソロ機能ポート=完了 / 残りは公証とコラボ対応・Apple Silicon(arm64)専用・Intel非対応**

- **実装済み**: macOS(arm64)ビルド一式 — PyInstaller ＋ `build_bundle.sh`
  （`Mojicast-<version>-mac-arm64.dmg` / `.zip` を生成・常に軽量版でモデルは
  `~/Library/Application Support/Mojicast/models` へ初回DL）、OS依存を platform_compat へ
  集約（フォント/CPU/言語/UIスケール/ダイアログ）、GitHub Actions の Win/mac 両CI
  （macos-14）、「はじめにお読みください（Mac版）」同梱。GUIは pywebview が
  自動で WKWebView を使うため WebView2ランタイム不要
- **残り1: 公証（notarization）** — 現状はフェーズ1として **ad-hoc署名のみ・未公証**で、
  初回起動に手動許可の手順が要る。Booth配布のUXを整えるなら Apple Developer Program
  **年99ドル**＋ビルドスクリプトに notarytool（自動化可）
- **残り2: コラボ非搭載** — proc_loopback.py は WASAPI プロセスループバック（純Windows）。
  やるなら BlackHole（仮想オーディオドライバ）＋保険で残してある
  collab_source=device 経路で対応
- Intel Mac非対応の理由: 2020年販売終了・macOS側もサポート終息へ・
  推論ライブラリの脱Intel mac進行・universal2ビルドの手間に見合わない

## 11. 複数翻訳の同時表示（構想メモ・寝かせ枠 2026-08-14）
**規模: 中（エンジン側に手が入る・Issue #2より大きめ）**

- 1発話に対して複数の翻訳先（例: 英語＋中国語）を同時に字幕表示する
- 実現方針は検討済み: `translate_lang` の複数化 → 翻訳ワーカーが翻訳先ごとに
  順次処理 → translation イベントに `lang` を追加 → overlay/コックピットで
  言語ごとに積む描画。FuguMT（英）＋M2M（他言語）は同時ロードで共存でき、
  M2M内の複数言語（中＋韓 等）は追加モデルなしで済む
- **寝かせている理由: CPU負荷**。翻訳は直列処理のため、翻訳先が1つ増えるごとに
  1発話あたりの翻訳時間が積み増しになる（2言語で約2倍）。低スペック機では
  キュー詰まり→訳文間引きが起きやすく、「余裕のあるPC向け機能」になる。
  併記時の画面圧迫（日本語＋2言語=3段）のデザイン判断も必要
- 表示モード「翻訳のみ」（v0.8.0・#2）の仕組みの延長で「日本語なし・英中2段」も可能

## 12. メモリ削減（実測ベース・2026-08-19）
**規模: 句読点＋翻訳のint8化=実装済み（2026-08-21・標準＋英訳で -419MB） / 残りは誘導文言**

ユースケース別の常駐メモリを実測した（`bench/bench_memory.py`・
結果表は [bench/README.md](bench/README.md)）。標準構成（日本語字幕）で
**741MB**、英訳ONで1167MB、中国語訳ONで1432MB、全部盛りで1801MB。

- **最大の的だった句読点BERT（単体+391MB）→ int8量子化を実装（2026-08-20）**。
  364MB→109MB・常駐 +390MB→+154MB（**-236MB**）・処理も約2倍速。標準構成は
  741MB→**507MB**、英訳ONでも 1167MB→**925MB** と1GBを切る（2026-08-21 実測）。
  変換は `tools/convert_models.py` の `quantize_punct()`
  （**per-channel必須**。per-tensorは判定一致50%で使い物にならない）。
  実配信ログ948行で fp32 と 89.5% 一致、不一致の7割は文末「。」の欠落で本文改変は0件
  （実測表は [bench/README.md](bench/README.md)）。fp32 は設定の「高精度モード」に
  残し、認識モデルの精度と同じスイッチで切り替わる
- 認識 fp32（+696MB）を選んでいる利用者は int8-fp32（+285MB）へ誘導すると -410MB。
  精度差はほぼ無し（v0.3.0の実測どおり）→ 初回おすすめ設定・AIモデル設定窓の文言で対応
- 多言語モード（SenseVoice）は句読点BERTを積まないので383MBと最軽量。
  低スペック機の逃げ道として案内価値がある（#6・軽量モード兼用の再確認）
- **英訳FuguMT（+460MB）→ int8化を実装（2026-08-21）**。CTranslate2 は実行時に
  compute_type を選べるためモデルの再変換・再配布は不要で、`float32`→`int8_float32`
  の1行。常駐 460MB→**309MB**（-151MB）・28ms→9ms（**3倍速**）。訳文はfp32と62%一致
  だが、fp32が文の後半を落とす例（「大切に使わせていただきます」の欠落）がint8では
  残るなど品質は互角。標準＋英訳は 1167MB→**748MB**、全部盛りは 1801MB→**1379MB**
- 翻訳M2M100（+727MB）は #11「複数翻訳の同時表示」の実現可否にも効く前提数字。
  **英訳が3倍速になったことで #11 の「寝かせ理由=CPU負荷」は大きく後退した**
  （2言語でも従来のfp32 1言語より速い計算になる）
- **M2M-100の「使う言語だけ切り出す」案 → 調査済み・見送り（2026-08-21）**。
  - 語彙の刈り込み: `model.bin` 468MB の内訳は 埋め込み131MB(28%)＋層337MB(72%)。
    削れるのは埋め込みだけで、層は100言語共有の本体なので切り離せない。日中に十分な
    48k語彙まで絞っても **-82MB 止まり**（実配信ログ8,737トークンでの使用語彙は
    1,457種＝全体の1.1%だが、未知語のため実運用でここまでは絞れない）
  - 言語ペア特化モデルへの置換（-600MB級を期待）は **ja→zh / ja→ko / ja→id の
    公式モデルが存在しない**ため不可（Helsinki-NLP にあるのは逆方向の tc-big-zh-ja 等）。
    日本語発は英語以外の選択肢が乏しく、**M2M-100 を使う現構成が正解**だと確認できた
  - ユーザー側での切り出しは torch が必要（v0.3.0で排除済み）で非現実的。やるなら
    開発側で言語別モデルを配る形になるが、翻訳先の変更ごとに別モデルDLが発生する
  - そもそも「使う言語だけ載せる」は **不要モデルの自動解放（v0.5.0・#6）で達成済み**。
    英訳だけの人はM2Mを積まないし、切替時に旧バックエンドは解放される
- 未計測: GUI窓（WebView2）の実使用ぶん。`bench/mem_watch.ps1` で起動中の
  プロセスツリーを合計して測れるようにしてあるので、窓数×メモリを別途取る
- 位置づけ: v1.0判断基準#3「CPU差の吸収」の姉妹課題。低スペック機（N100・8GB機）の
  実用性はCPUだけでなくメモリでも決まる

## 13. NPU活用（構想メモ・寝かせ枠 2026-08-21）
**規模: 現状は着手不可（sherpa-onnx の対応待ち）**

認識をNPUへ逃がしてCPUを空けられれば、配信中のゲーム・エンコードとの競合が減る。
v1.0判断基準#3「CPU差の吸収」の将来枠。

- **現状は不可**。認識（k2/SenseVoice）は sherpa-onnx 経由で、受け付ける provider は
  **cpu / cuda / coreml のみ**（2026-08-21 時点）。`asr_model.py` には `provider=device` を
  渡す口が既にあるので、sherpa-onnx が NPU EP を持てば渡すだけで動く可能性はある
- ONNX Runtime 自体は NPU 対応が進んでいる（Intel=OpenVINO EP / Qualcomm=QNN EP /
  汎用=DirectML EP）。ただし pip の onnxruntime は CPUビルドのため専用ビルドが必要で、
  配布サイズが膨らむ。NPUへ逃がせるのは句読点BERT（pip ORT直叩き）だけだが、
  int8化済みで1行5msのため効果はほぼ無い。翻訳のCTranslate2も cpu/cuda のみ
- **Windows の音声認識APIを使う案は不採用**: Windows AI Foundry(Copilot+ PC) に
  音声認識は含まれず（Phi Silica・OCR・画像系が中心）、ライブキャプションのエンジンは
  非公開。`Windows.Media.SpeechRecognition` は旧世代で、日本語精度・句読点・
  ホットワードのどれも現構成に劣る。Mojicastの強み（日本語特化＋認識誘導）を失う
- **再検討のトリガー**: sherpa-onnx が NPU EP をサポートしたとき。NPU搭載機の普及も
  見てから判断する（2026-08時点では先行きが読めない）

## 14. 句読点の設定を分かりやすく（調査 2026-08-22・v1.0の刷新枠）

**規模: 小（設定UI）/ v1.0判断基準#2「設定の分かりやすさ」の具体例**

きっかけ: 「アプデしたら全部『。』が入るようになった。どっかにないかな句読点みたいな設定」
という声。調査の結果、**アプデによる変化ではなく「初めて使う人が最初に驚くポイント」**だった
（投稿者は入れてすぐアプデしており旧版の使用時間がほぼ無い。初回のモデルDL完了と
アプデが重なったと見られる）。設定は**存在する**（設定 → その他 → 字幕処理 →
「句読点を付ける」）が、**「。を減らしたい」という動機からたどり着けない**。
マニュアルにも設定一覧の1行があるだけで、困ったとき欄には無い。
**結論（2026-08-22・えもさん判断）: コード側の対応は不要。「気になる人は句読点チェックを
外してね」で足りる。** 本丸は設定の発見性なので、v1.0のUI刷新と下記#15のマニュアル改定で扱う。

以下は原因調査の結果。モデル側の対策は見送ったが、結論は再調査しなくて済むよう残す。

- **int8化（v0.9.5）は無関係。むしろ逆方向**。実配信ログ1043行で fp32 vs int8 を全行比較
  した結果、一致90.0%・「。」は **856→759個（-97）**・行末の「。」は 737→665行
  （70.7%→63.8%）。不一致104行のうち **int8で。が増えたのは1行だけ・減ったのが97行**。
  v0.9.5は旧版より句点が11%少ないバージョンで、苦情の原因になり得ない
- **原因は句読点BERTの末尾バイアス**。学習データが常に完結文（末尾に必ず「。」）
  だったため、モデルは「[SEP]の直前＝文末」というポジションを覚えており、文が
  完結しているかを見ていない。同じ位置の「。」確率が、末尾だと 0.971、続きを足して
  文中にすると 0.000（「ここでちょっと」＋「休憩しますね」）。5桁変わる
- **しきい値では分離不能**。言い途中の「ここでちょっと」が 0.971 で、完結文の
  「わかりました」0.939 より高い。断片10件/完結文10件の判別率は 50%＝コイントス
- **VADが切るのは無音300msの息継ぎ**であって文の終わりではない（`silence_ms`）。
  言い途中の行が大量に出るのは構造的
- 検討した対策と成績（判別率）: 末尾にダミー語「そして」を足して判定 **85%** /
  [SEP]を外す 70〜80% / 末尾に[MASK]×8 75%。**ダミー語は成績が最良だが、話者が
  言っていない語を入力に混ぜることになり、行内の読点判定への副作用も説明できない
  ため見送り**（2026-08-22 判断）
- **細切れ発話では句読点機能が実質「行末に。を付けるだけ」になっている**。配信想定の
  連続21行で、行末の「。」21行に対し**行内の「。」1個・読点0個**。1行が読点を打つ
  長さに達しないため。＝この使い方なら設定をOFFにしても失うものはほぼ無い
- 将来やるなら「行末の。は打たない（文脈のある行内だけ打つ）」が筋。言っていない語も
  特殊トークンも使わず、行の切れ目が区切りを兼ねる。設定の選択肢として持たせる案も

関連: 読点(、)側の限界は [句読点の読点は改善不能] の調査を参照（文脈/閾値/ガードの
4手すべて無効・文字単位BERTの構造的限界）。

## 15. マニュアル・ガイドの改定（使いやすさ重視の再構成）

**規模: 中 / v1.0判断基準#2「設定の分かりやすさ」の一部**

現状のマニュアルは**機能の一覧・設定の羅列**が中心で、「〜したい」「〜が気になる」という
**利用者の動機から引けない**。#14 の句読点の件がその実例で、設定は
[マニュアル.html] の設定一覧に1行あるが、「。が多いので減らしたい」と思った人は
そこへ到達できなかった（設定画面を既に開いている人しか読まない位置にある）。
「困ったとき」欄にもモデルのロード失敗は載っているが、**見た目の好みに関する項目が無い**。

- 方向性: 目的ベースの導線を足す（「字幕の見た目を変えたい」「認識がおかしい」
  「重い」「。が多い」→ 触る場所へ誘導）。機能一覧は残しつつ入口を増やす
- 「困ったとき」を**不具合だけでなく好みの調整も含む**構成へ広げる
- 初見の人がつまずく点を洗い出す（初回モデルDLの待ち時間・句読点・VADの切れ方など。
  Discordやテスターの声を素材にする）
- ガイド（スタイル/エフェクト/テクニカル）との役割分担も整理する
- v1.0のUI刷新と同時期にやると、画面と文章の食い違いが出ずに済む

## 16. 動画・音声ファイルから字幕を作る（構想メモ・寝かせ枠 2026-08-24）

**規模: フェーズ0=小 / 読み込み＋編集UI=中〜大 / 新規依存は「音声の取り出し」だけ**

出発点の思いつき: **動画を読み込めれば、字幕を「直せる」**。リアルタイムの制約
（間に合わせる・言い直せない・その場限り）が全部消えるので、同じパイプラインが
別の道具になる。配信のアーカイブ・切り抜き・過去回の字幕付けが射程に入る。

**なぜMojicastでやると強いか**

- 既存資産がそのまま効く: ホットワード（認識誘導）・置換辞書・句読点・数字の
  算用数字化・禁止ワード・翻訳・字幕デザイン。汎用の文字起こしツールに無い
  「**その配信者の語彙**」が最初から入っている状態で書き起こせる
- 完全オフライン。長尺の音声を外に上げなくていい（クラウド書き起こしの一番の壁）
- 「配信する→アーカイブに字幕を付ける」が同じアプリで地続きになる

**分かっていること（コードを読んで確認・2026-08-24）**

- **認識パイプラインはほぼそのまま使える**。入力は `engine._stream_loop` の
  `open_stream()`（engine.py:879）が「入力デバイス / プロセスループバック」を
  出し分けている構造で、**3つ目のソースとして「ファイル」を足す形**になる。
  VAD→認識→置換→数字→句読点→禁止ワード→翻訳の下流は共通のまま
- **前例がある**: `bench/probe_emotion_live.py` の `run_wav`（:381）が既に
  wav→VAD→認識を回している（感情調査用に書いたもの）
- **タイムコードは追加コスト無しで取れる（重要）**。今使っている sherpa-onnx の
  VADセグメントは `samples` に加えて **`start`（ストリーム先頭からのサンプル位置）**
  を持つ。実測で確認（sherpa-onnx 1.13.4 / `silero_vad.onnx` / 合成音）:
  開始 = `start / 16000` 秒、終了 = `(start + len(samples)) / 16000` 秒。
  SRT/VTT の時刻はこれで足りる。※現状の `_log_final`（engine.py:222）は
  確定した実時刻を書くだけで、**発話の開始秒は持っていない**
- **実時間より速く回せる**見込み。ファイル入力なら (1) 無音を待たずに送れる
  (2) 途中経過（薄文字）のデコードが要らない (3) 適応スロットリング
  （engine.py:1012）も要らない。倍率は要実測（CPUで数倍違うので
  「適性◎で何倍」の形で出す）

**新規に要るのは音声の取り出しだけ**（mp4/mov/mkv → 16kHz mono float32）

- 案A: **ffmpeg 同梱**。確実だが配布サイズ（現状 Zip 82MB）に響き、
  ライセンス表記も増える
- 案B: **OSのデコーダを ctypes で叩く**（Windows = Media Foundation
  SourceReader / mac = AVFoundation・AudioToolbox）。追加バイト0。
  `proc_loopback.py` で WASAPI を純ctypesで実装した前例があるので、
  この家の作法に合う。**まずはBの調査から**（mp4の音声をPCMに落とせるか・行数）
- 案C: 最小版として音声ファイルのみ（soundfile で wav/flac/ogg）。
  「録画から音だけ抜いてくる」手間を利用者に押し付けるので入口としては弱い

**フェーズ0（小・単独で価値がある）: 配信中に SRT も残す**

動画読み込みが無くても、**今の配信からアーカイブ用の字幕が作れる**。
`_stream_loop` は VADセグメントを持っているので、セッション開始からのオフセット
（`start`）と確定文を `logs/` に `.srt` としても吐けばいい。OBSの録画開始と
セッション開始はずれるため、書き出し時に**オフセット秒の調整欄**（±で頭出しを
合わせる）を付ける。これだけで「配信の録画に字幕を焼く」が回り、
読み込み側を作る前に**需要の有無を確かめられる**。

**フェーズ1: 読み込み → 一括文字起こし**

- ファイル選択はネイティブダイアログ（pywebview の `js_api` は app.py:318 に
  既にある。ブラウザの `<input type=file>` ではパスが取れないのでこちらを使う）
- 進捗（何%・残り時間）を出す。長尺は数分かかる前提のUIにする
- 話者は1人前提から。コラボ（2話者）の分離は後回し

**フェーズ2: 編集UI**

- 最低限は「行の編集・分割・結合・時刻の微調整・削除」。字幕ログ画面
  （cockpit の monitor）の発展形として作れる
- **その行だけ再認識**（ホットワードを足してから効かせ直す）は他に無い強み。
  認識結果ではなく**音声が手元に残っている**からできる
- 動画と並べたプレビューは `broadcast()`（app_server.py:592）で overlay に
  流せば、いつもの字幕デザインのまま見える
- 書き出しは SRT / VTT（YouTube・Premiere・DaVinci が読める）。**焼き込みはしない**
  （ffmpeg依存が増える上に、編集ソフトの領分）

**慎重に考えるところ**

- **アプリの性格が変わる**。Mojicastは「配信中にリアルタイムで出す」道具で、
  編集ツールは別の期待値（正確さ・作業効率・やり直し）を呼ぶ。v1.0の判断基準
  （実戦フィードバック・UI最適化・CPU差の吸収・配布形態）とは別軸なので、
  **v1.0前に入れると焦点がぼける**
- 字幕編集そのものは既存ツール（Aegisub / Subtitle Edit / 各編集ソフト）が強い。
  こちらが勝てるのは「自分の語彙で認識の質を上げられる」「オフライン」
  「配信の続きでできる」の3点で、編集機能の作り込みで張り合わないほうがいい
  （**SRTで出して編集は好きなソフトで**、が現実的な落としどころ）
- 長尺のメモリ: 1時間の16kHz float32 で約230MB。全部抱えず、セグメント単位で
  捨てる設計にする（再認識用に元音声が要るなら、ファイルへ読みに戻る作り）

**次の一歩**: フェーズ0（SRT書き出し）だけ先に出して、テスターが実際に
アーカイブ字幕を使うかを見る。使われるなら読み込み側へ進む。

---

## 済んだもの（参照用）

- v0.5.0（2026-07-22）: **多言語対応** ＝ SenseVoice多言語認識（中英日韓粤・
  モデルレジストリ受け皿・軽量モード兼用: 13700HXで翻訳込み13〜20%、N100条件付き動作）・
  翻訳4方向（FuguMT＋M2M-100 int8、認識言語×翻訳先の経路自動選択、HF配布
  `ishiki-emo/mojicast-m2m100-ct2`）・AIモデル設定窓・初回おすすめ設定（OS言語/CPU検出・
  ユーザー了解制）・読み欄「／」複数形置換＋1パス化・不要モデルの自動解放・
  再起動警告バー（今すぐ反映ボタン）・単語エフェクトチップ・スピンウェイト抑制・
  ドキュメント再編（配信者目線マニュアル＋スタイル/エフェクト/テクニカルガイド同梱・逆引き）・
  スモークに多言語経路検証を追加

- v0.4.0（2026-07-20）: **1対1コラボ字幕（案A改）** ＝ WASAPIプロセスループバックで
  通話アプリ（Discord等）の音だけを直接取り込み（純ctypes実装・仮想ケーブル不要・
  相手の準備不要）・話者別2ストリーム認識（モデル1ロード・単一Recognizer直列・fid一意）・
  話者別のスタイル/レイアウト割当＋overlay複数ペイン描画・字幕ログの話者チップ・
  コラボ用既定スタイル（コラボ(小さめ)/左右ハーフ・既存環境へのseed配布機構）・
  負荷対策（薄文字は末尾6秒のみデコード＋デコード時間に応じた適応スロットリング）・
  /api/perf 診断・マニュアル全面改定（実画面スクショの自動生成パイプライン
  tools/manual/）・README/docs/MANUAL.md 整備
- v0.3.1（2026-07-19）: 日本語名フォルダにインストールすると英訳モデルの
  ロードに必ず失敗する問題を修正（SentencePiece が非ASCIIパスを開けない
  Windows固有の制限 → model_proto のバイト列渡しで回避）・句読点/英訳の
  ロード失敗時に traceback を logs\load_error.log へ記録（調査用）
- v0.3.0（2026-07-13）: 単語プロファイル＋data/フォルダ再編（自動移行つき）・
  スタイルのエクスポート/インポート（mojipack先行分・HTML注入対策込み）・
  推論ランタイム載せ替え（句読点=ONNX Runtime / 翻訳=CTranslate2・torch排除・
  Zip 320→178MB・初回DL 2GB→1.2GB。変換は `tools/convert_models.py`、配布は
  HF `ishiki-emo/mojicast-punct-onnx` と `mojicast-fugumt-ja-en-ct2`〔CC BY-SA継承〕）・
  OBSポートのUI編集＋衝突時MessageBox・レインボー/シャインの視認性改善
- v0.2.0（2026-07-11）: 英訳辞書・英訳スタイル調整・1080p忠実プレビュー・UI刷新・
  上書きアップデート安全化（データをZip非同梱に）・スペック定義・smoke_test 運用開始
- v0.1.0（2026-07-10）: 初回リリース。字幕/句読点/英訳/ログ/禁止ワード/MOTW自己解除

---

## 不具合: 稼働中に字幕が無言で全消失する（調査 2026-08-22・v0.9.6 で修正）

外部配信での実例（翻訳のみ表示・v0.9.0）で、配信の途中から30分〜1時間ほど字幕が
出なくなった。エンジンは「認識中」のままで、GUIにもログにも異常は出ない。
再起動で復帰。原因は特定できていないが、**そうなり得る経路が複数あり、どれも
配信者が気づけない**ことが分かったので、経路ごと塞ぐ。

### 根本にあった前提の崩れ

`engine._translate_loop` の `except Exception: en = ""  # 翻訳失敗は無視（字幕本体は
出続ける）` は initial commit から在るが、このコメントが正しいのは**併記モードだけ**。
v0.8.0 で追加した翻訳のみ表示（#2）には「字幕本体」が無く、**訳＝字幕**なので、
訳が出せない＝画面が空になる。v0.8.0 の `dc9d794` は `translating()` で
「起動時に翻訳が死んでいる」ケースを併記へ逃がしたが、**稼働中に死ぬケースは
対象外**だった（`translating()` は設定とロード結果の判定であって、実行時の健全性を
見ていない）。

### 塞いだ経路

1. **翻訳の実行時失敗・空文字** → `on_translation(fid, 原文, fallback=True)` を必ず
   通知し、表示側でその行だけ原文へ切り替える。英訳辞書の置換は翻訳の前処理なので、
   フォールバックには置換前の原文を使う（英単語混じりの日本語を出さない）
2. **翻訳キューの間引き** → 押し出された行にも原文フォールバックを出す
   （`_offer_bounded_latest` が捨てた「中身」を返すように変更）
3. **オーバーレイ側のラッチ** → `PENDING_MAX` が 64 で翻訳キュー上限 128 より小さく、
   訳が届く前に待ち行が捨てられると「訳が来ても表示先が無い」状態で固定されていた。
   192 へ引き上げ、キュー上限より必ず大きいことをテストで固定
4. **入力の無言死** → `sd.InputStream` のコールバックが来なくなっても例外にならず、
   `queue.Empty` を回り続けるだけで状態は running のままだった（`stream.error` を
   見る既存の検査は `ProcessLoopbackCapture` 専用で、マイク経路では素通り）。
   最終受信時刻を持ち、5秒途絶で警告＋ストリームを開き直す
5. **無言の間引き** → `_offer_bounded_latest` の戻り値を両方の呼び出し元で捨てて
   いたため、取りこぼしがどこにも残らなかった。`perf` に `audio_dropped` /
   `audio_overflow` / `translate_dropped` / `translate_fail` として計上（`status`
   引数も同様に捨てていたので、input_overflow を数える）

### 残っている宿題

- コックピットが `/api/perf` を読んでいないので、上の計測値がまだ画面に出ない。
  「間引き中」の常時表示は次の課題（警告バーの器はできている）
- 実際の原因の特定には配信者側の `logs/` と `translate_error.log` が要る

## 不具合: 古いCPUで句読点が全文字に付く（調査 2026-08-22・v0.9.6 で対策）

v0.9.5 へ上げた利用者（Win11 / **i9-9900K** / OBS字幕＋翻訳）から「字幕が
『あ。い。う。え。』と全文字に句点が付く」報告。字幕ログでもテキスト自体が
そうなっていた（表示側ではない）。

### 機序（再現で確定）

句読点モデルの推論結果が0付近になると `sigmoid(0)=0.5` がしきい値 `period_thresh=0.1`
を全文字で超え、**全部に「。」を打つ**。推論を全ゼロに差し替えて模擬したところ、
報告ログと1文字も違わず一致した。**モデルが黙って死ぬと「打たない」ではなく
「全部に打つ」に倒れる**設計だった。

原因は **v0.9.5 の int8 化**（#12・それ以前は fp32 なので発症しない）。量子化は
U8S8・`per_channel=True`・`reduce_range` 未指定で、この構成は **VNNI 非搭載CPU
（9900K=Coffee Lake）だと積和が int16 で飽和して出力が崩れる**。切り分け:

- HF配布ファイルは開発機の正常動作するファイルと sha256 一致 → **DL破損ではない**
  （利用者の再ダウンロードでも改善せず、これで確定）
- **開発機（Ryzen 9800X3D＝VNNI あり）では絶対に再現しない**。量子化を入れるときは
  古いCPUでの確認が要る、という運用上の穴

### 入れた対策（実装済み）

壊れても**句読点だけ諦めて字幕は守る**。`punct.py`:

- **ロード時の自己診断を強化** — 従来は「出力が空でないこと」しか見ておらず、
  壊れた出力（`こ。れ。は。て。す。と。で。す。`）は空でないので合格していた。
  句読点が湧きすぎたらロード失敗とし、`engine` 側の既存の逃がし（句読点だけ無効化）に乗せる
- **実行中のガード** `_looks_broken()` — 挿入数が文字数の半分を超えたら（かつ3個以上）
  原文を返す。短文（「はい。」）は巻き込まない
- 生の付与を `_punctuate_raw()` に分離。ガード後の結果では「壊れて全部打った」と
  「1個も打たなかった」を区別できず、**自己診断がガードに隠れて機能しなくなる**ため
- `tests/test_punct_guard.py` で固定。実配信ログ1405行で誤発火0件（int8/fp32とも）

### 自動 fp32 フォールバック（v0.9.6 で追加）

上のガードだけだと該当CPUでは「句読点なし」で確定してしまう。当初は警告文で
「設定の高精度モードで回避できます」と案内する形にしたが、**該当環境からの
問い合わせが減らなかった**ため踏み込んだ。自己診断に落ちたら fp32 で開き直す。

- 専用の例外 `punct.SelfTestFailed` で「読み込み失敗」と「結果が壊れている」を分ける
  （従来の `句読点の読み込みに失敗` は、実際には読み込めているのでズレていた）
- 実測は int8 が **+147MB / 5.9ms per文**、fp32 が **+367MB / 11.7ms per文**。
  **既定を fp32 にすると全員が 220MB 損をする**ので、壊れた環境だけ寄せる
- 切り替えたことは警告に出す。黙って重くすると原因不明の不調に見えるため
- VNNI 非搭載CPUが手元に無いので、壊れ方を模した `_FakePunct` で経路を固定した
  （int8が動くときは fp32 を触らない／両方壊れたら従来どおり句読点なしで続行、など5経路）

これで該当CPUの利用者は**何もしなくても句読点が出る**。メモリは +220MB 増えるが、
理由はステータスに出る。

### 残る選択肢（未着手）

**`reduce_range=True` で int8 を再量子化して HF に上げ直す** — 本命。重みを7bitに
するので飽和せず、**109MBのままメモリ削減を維持できる**（自動 fp32 は該当環境で
+220MB になる）。精度差は再量子化して実配信ログで実測する必要あり。効果の確認は
開発機では不可能（VNNI があるので元々壊れない）→ 9900K の報告者にファイルを
差し替えてもらうのが唯一の検証手段。モデルの再配布を伴うので、リリースを分けて扱う。

### #14 との関係（要注意）

#14 の「アプデしたら全部『。』が入るようになった」は**設定の発見性の問題として
結論づけたが、同じ不具合だった可能性がある**（同じ 2026-08-22・同じ v0.9.5 への
アプデ直後・症状の言い回しも一致）。#14 の投稿者のCPUが VNNI 非搭載なら誤診。
確認できるなら機種を聞く価値がある。
